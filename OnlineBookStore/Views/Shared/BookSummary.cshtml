@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Book
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>@Model.Title</h4>
        <span class="card-header-pills">
            <button type="submit" class="btn btn-success float-end" style="background-color:saddlebrown">
                @Model.Price.ToString("c")
            </button>
            
        </span>
    </div>
    <div class="card-text p-1">Category: @Model.Category</div>
    <div class="card-text p-1">Author: @Model.Author</div>
    <div class="card-text p-1">Publisher: @Model.Publisher</div>
    <div class="card-text p-1">ISBN: @Model.Isbn</div>
    <div class="card-text p-1">@Model.PageCount pages long</div>

    <form asp-page="/ShoppingCart" method="post">

        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />

        <span class="card-text">
            <button type="submit" class="btn btn-success float-end">
                Add to Shopping Cart
            </button>
        </span>
    </form>
</div>
